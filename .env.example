# Backend Environment Configuration
PORT=3001
NODE_ENV=development

# Prometheus Service URL
# Local development default (when port-forwarding Prometheus):
PROMETHEUS_URL=http://localhost:9090

# In-Cluster Kubernetes default (used by Deployment environment):
# PROMETHEUS_URL=http://monitoring-kube-prometheus-prometheus.monitoring:9090
